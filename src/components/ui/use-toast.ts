
// Re-export from our hooks for convenience and to maintain backward compatibility
import { useToast, toast } from "@/hooks/use-toast";
export type { Toast } from "@/hooks/use-toast";

export { useToast, toast };
